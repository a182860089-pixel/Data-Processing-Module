# 阶段 3.5 前端接入与可视化测试方案

## 📋 目录
1. [现状分析](#现状分析)
2. [前端接入方案](#前端接入方案)
3. [可视化测试指南](#可视化测试指南)
4. [集成步骤](#集成步骤)
5. [测试清单](#测试清单)

---

## 现状分析

### 后端完成情况 ✅
- **多格式转 PDF**：Office 文档（DOCX/PPTX/XLSX）、图片（JPG/PNG/GIF 等）
- **API 端点**：`POST /api/v1/convert` - 统一转换接口
- **转换选项**：支持 DPI、页面大小、缩放模式等参数
- **文件检测**：自动识别文件类型

### 前端现状 🎨
- **已有功能**：
  - ✅ 图片压缩（WebP）功能完整
  - ✅ PDF→Markdown 转换界面完整
  - ✅ 标签页切换 UI
  - ✅ 后端服务状态监控
  - ✅ 错误/成功提示

- **缺失部分**：
  - ❌ Office 文件上传/转换 UI
  - ❌ 多格式文件上传界面
  - ❌ 转换参数配置面板
  - ❌ 可视化进度显示

---

## 前端接入方案

### 方案一：扩展现有 PdfUploader（推荐 - 快速接入）

在 `PdfUploader.vue` 基础上，支持多种文件格式：

```typescript
// 支持的文件类型
const SUPPORTED_FORMATS = {
  pdf: { ext: '.pdf', mime: 'application/pdf', label: 'PDF' },
  // Office 文档
  docx: { ext: '.docx', mime: 'application/vnd.openxmlformats-officedocument.wordprocessingml.document', label: 'Word' },
  pptx: { ext: '.pptx', mime: 'application/vnd.openxmlformats-officedocument.presentationml.presentation', label: 'PowerPoint' },
  xlsx: { ext: '.xlsx', mime: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet', label: 'Excel' },
  // 图片
  jpg: { ext: '.jpg', mime: 'image/jpeg', label: 'JPEG' },
  png: { ext: '.png', mime: 'image/png', label: 'PNG' },
  gif: { ext: '.gif', mime: 'image/gif', label: 'GIF' },
};
```

**优点**：
- 最小化改动，快速集成
- 复用现有 UI 框架
- 降低测试成本

**实现步骤**：
1. 修改文件选择接受规则
2. 根据文件类型设置不同的转换选项
3. 结果展示时适配不同格式

---

### 方案二：创建独立的 MultiFileUploader 组件（推荐 - 功能完整）

创建新的 `MultiFileUploader.vue` 组件，支持批量上传多种格式：

#### 核心特性
- 同时支持 PDF、Office、图片上传
- 拖拽上传多个文件
- 实时进度显示
- 转换参数自定义
- 结果预览与下载

#### 文件结构
```
src/components/
├── MultiFileUploader.vue      # 主上传组件
├── OfficeUploader.vue         # Office 专用（可选）
├── FilePreview.vue            # 通用预览组件
└── ConvertOptionsPanel.vue    # 转换参数配置
```

#### 核心逻辑
```vue
<script setup lang="ts">
import { ref } from 'vue'

interface FileItem {
  id: string
  file: File
  type: 'pdf' | 'office' | 'image'
  status: 'pending' | 'processing' | 'completed' | 'failed'
  progress: number
  result?: ConversionResult
  error?: string
}

interface ConversionResult {
  markdown?: string
  taskId: string
  downloadUrl: string
  metadata: Record<string, any>
}

// 接受的文件类型
const ACCEPT_TYPES = '.pdf,.docx,.pptx,.xlsx,.jpg,.jpeg,.png,.gif'

const files = ref<FileItem[]>([])

// 根据文件扩展名判断类型
const detectFileType = (file: File): FileItem['type'] => {
  const ext = file.name.split('.').pop()?.toLowerCase()
  if (ext === 'pdf') return 'pdf'
  if (['docx', 'pptx', 'xlsx'].includes(ext)) return 'office'
  if (['jpg', 'jpeg', 'png', 'gif'].includes(ext)) return 'image'
  throw new Error('不支持的文件格式')
}

// 构建转换选项
const buildConvertOptions = (fileType: FileItem['type']) => {
  const baseOptions = { dpi: 144 }
  if (fileType === 'office') {
    return { ...baseOptions, keep_layout: true, office_dpi: 96 }
  }
  if (fileType === 'image') {
    return { ...baseOptions, page_size: 'A4', fit_mode: 'fit' }
  }
  return baseOptions
}

// 上传并转换
const convertFile = async (item: FileItem) => {
  item.status = 'processing'
  const formData = new FormData()
  formData.append('file', item.file)
  formData.append('options', JSON.stringify(buildConvertOptions(item.type)))

  try {
    const resp = await fetch(`${API_BASE_URL}/api/v1/convert`, {
      method: 'POST',
      body: formData
    })
    
    if (!resp.ok) throw new Error(`转换失败: ${resp.status}`)
    
    const data = await resp.json()
    item.result = {
      markdown: data.markdown_content,
      taskId: data.task_id,
      downloadUrl: data.download_url,
      metadata: data.metadata
    }
    item.status = 'completed'
  } catch (error) {
    item.status = 'failed'
    item.error = error instanceof Error ? error.message : '转换失败'
  }
}
</script>
```

---

## 可视化测试指南

### 测试环境准备

#### 1. 启动后端服务
```bash
cd "D:\Data Processing Module\data_to_md-main"

# 安装依赖（如果尚未安装）
pip install -r requirements.txt

# 启动 FastAPI 服务
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

#### 2. 启动前端服务
```bash
cd "D:\Data Processing Module\proc_image\smallimg"

# 安装依赖（如果尚未安装）
npm install

# 启动开发服务器
npm run dev
```

前端会运行在 `http://localhost:5173`

---

### 测试场景

#### 场景 1：图片转 PDF（端到端）

**预期行为**：
1. 上传 JPG/PNG 图片
2. 前端调用 `/api/v1/convert` API
3. 后端 ImageToPDFConverter 处理
4. 返回转换后的 Markdown（或 PDF 下载）
5. 前端显示转换结果

**测试步骤**：
```
1. 浏览器打开 http://localhost:5173
2. 点击"PDF → Markdown"标签（或新增"多格式转换"标签）
3. 拖拽或选择一张 JPG 图片
4. 点击"开始转换"
5. 观察进度条和最终结果
```

**验证点**：
- ✅ 文件上传成功
- ✅ API 调用正确（F12 开发者工具 → Network）
- ✅ 转换不报错
- ✅ 结果正确显示（markdown_content 字段）

---

#### 场景 2：Office 转 PDF（端到端）

**预期行为**：
1. 上传 DOCX/PPTX/XLSX 文件
2. 前端调用 `/api/v1/convert` API
3. 后端 OfficeConverter 处理
4. 返回转换后的 Markdown
5. 前端显示转换结果

**测试步骤**：
```
1. 浏览器打开 http://localhost:5173
2. 选择 Office 文件（DOCX/PPTX/XLSX）
3. （可选）配置转换参数：keep_layout, office_dpi
4. 点击"开始转换"
5. 观察转换进度和结果
```

**验证点**：
- ✅ 文件上传成功
- ✅ 后端日志显示转换过程
- ✅ 返回 markdown_content
- ✅ 下载功能正常

---

#### 场景 3：图片缩放模式测试（仅图片）

**预期行为**：
- `fit`：保持宽高比，完全适应
- `contain`：保持宽高比，适应容器
- `cover`：覆盖整个页面
- `stretch`：拉伸填充

**测试步骤**：
```
1. 上传宽图片（如 2000×800）
2. 在参数面板选择不同的 fit_mode
3. 观察转换结果（可从下载的 PDF 验证）
```

---

#### 场景 4：并发转换（批量处理）

**预期行为**：
- 同时上传多个文件
- 前端显示进度列表
- 后端逐个或并发处理

**测试步骤**：
```
1. 在 MultiFileUploader 中同时选择 5 个文件
   - 2 张图片
   - 1 个 DOCX
   - 1 个 PNG
   - 1 个 PPTX
2. 点击"批量转换"
3. 观察进度面板
4. 验证所有文件都转换成功
```

---

### 测试工具与技巧

#### 使用浏览器开发者工具

**Network 标签**：
```
1. F12 打开开发者工具
2. 切换到 Network 标签
3. 进行转换操作
4. 查看请求/响应：
   - 请求 URL：http://localhost:8000/api/v1/convert
   - 请求方法：POST
   - 响应状态：200
   - 响应体：{ success, task_id, markdown_content, ... }
```

**Console 标签**：
```
// 检查 API 响应
console.log('前端接收到的数据:', data)

// 手动调用 API 测试
fetch('http://localhost:8000/api/v1/convert', {
  method: 'POST',
  body: formData
}).then(r => r.json()).then(d => console.log(d))
```

**后端日志**：
```
监控后端窗口输出，查看：
- 文件接收日志
- 转换器初始化
- 转换过程日志
- 结果生成
```

---

### 性能测试

#### 单个文件转换时间
```
指标: 处理时间 (processing_time)

预期:
- 小图片（<1MB）: 1-3 秒
- Office 文档（<5MB）: 3-10 秒
- PDF（<10MB）: 5-15 秒
```

#### 批量转换
```
指标: 并发处理能力

测试:
1. 同时上传 10 个小文件
2. 记录总耗时
3. 验证每个文件都成功

预期:
- 总耗时 < 30 秒
- 成功率 100%
```

---

## 集成步骤

### 第 1 步：更新前端接受文件类型

编辑 `src/components/PdfUploader.vue`（或新建 MultiFileUploader）：

```vue
<!-- 修改文件选择器 -->
<input 
  type="file" 
  accept=".pdf,.docx,.pptx,.xlsx,.jpg,.jpeg,.png,.gif"
  multiple
  @change="handleFileChange"
/>
```

### 第 2 步：添加文件类型检测

```typescript
const detectFileType = (filename: string) => {
  const ext = filename.split('.').pop()?.toLowerCase()
  const typeMap = {
    pdf: 'PDF',
    docx: 'Word',
    pptx: 'PowerPoint',
    xlsx: 'Excel',
    jpg: 'Image',
    png: 'Image',
    gif: 'Image'
  }
  return typeMap[ext] || 'Unknown'
}
```

### 第 3 步：根据文件类型设置转换选项

```typescript
const buildOptions = (fileType: string) => {
  switch (fileType) {
    case 'Word':
    case 'PowerPoint':
    case 'Excel':
      return { keep_layout: true, office_dpi: 96 }
    case 'Image':
      return { page_size: 'A4', fit_mode: 'fit' }
    case 'PDF':
    default:
      return { dpi: 144, include_metadata: false }
  }
}
```

### 第 4 步：调整结果显示

```typescript
// 根据文件类型显示不同的结果
if (fileType === 'Image') {
  // 显示转换为 PDF 的预览或下载链接
} else if (['Word', 'PowerPoint', 'Excel'].includes(fileType)) {
  // 显示转换为 PDF 的预览或下载链接
} else if (fileType === 'PDF') {
  // 显示 Markdown 内容
}
```

---

## 测试清单

### 功能测试
- [ ] PDF 上传和转换
- [ ] DOCX 上传和转换
- [ ] PPTX 上传和转换
- [ ] XLSX 上传和转换
- [ ] JPG/PNG 上传和转换
- [ ] GIF 上传和转换
- [ ] 多文件同时上传
- [ ] 大文件上传（>10MB）
- [ ] 文件验证（错误的文件格式）
- [ ] 错误处理和提示

### 界面测试
- [ ] 上传区域 UI 正常
- [ ] 进度条显示准确
- [ ] 成功/错误提示出现
- [ ] 结果正确显示
- [ ] 下载功能正常
- [ ] 响应式设计（手机、平板）

### 性能测试
- [ ] 小文件转换耗时 < 5 秒
- [ ] 批量上传 5 个文件耗时 < 30 秒
- [ ] 浏览器内存使用稳定（无明显泄漏）

### 兼容性测试
- [ ] Chrome 浏览器
- [ ] Firefox 浏览器
- [ ] Safari 浏览器
- [ ] Edge 浏览器
- [ ] 手机浏览器（iOS Safari、Android Chrome）

---

## 推荐实现优先级

### 第 1 阶段（快速验证，1-2 天）
1. ✅ 扩展 `PdfUploader.vue` 支持 Office 文件
2. ✅ 添加基本的转换参数配置
3. ✅ 后端 API 测试

### 第 2 阶段（完整功能，3-5 天）
1. 创建 `MultiFileUploader.vue` 组件
2. 实现拖拽批量上传
3. 添加进度显示和结果预览
4. 集成下载功能

### 第 3 阶段（优化和完善，5+ 天）
1. 性能优化
2. 错误处理完善
3. UI/UX 优化
4. 完整的自动化测试

---

## 常见问题

### Q1: 转换时间太长怎么办？
**A**: 
- 检查后端日志，确认瓶颈
- 如果是 OCR（PDF→Markdown），考虑使用 MinerU（阶段4）
- 增加并发处理能力

### Q2: 前端无法连接后端？
**A**:
- 确保后端服务运行：`python app/main.py`
- 检查 API_BASE_URL 配置
- 查看浏览器 Console 的 CORS 错误

### Q3: Office 文件转换失败？
**A**:
- 检查依赖是否安装：`pip install python-docx python-pptx openpyxl`
- 查看后端日志中的错误信息
- 尝试用最新版本 Office 格式保存文件

### Q4: 如何实现实时进度显示？
**A**:
- 使用 WebSocket（需要后端支持）
- 或使用轮询 `/api/v1/status/{task_id}` 接口

---

## 总结

✅ **后端已完全就绪**，3.5阶段的多格式转换功能已实现
✅ **前端可快速接入**，最小改动即可支持 Office/图片上传
✅ **可视化测试简单**，使用现有 UI 框架和浏览器工具

**下一步建议**：
1. 运行后端服务，验证 API 功能
2. 扩展前端 UI 支持多种文件格式
3. 进行端到端测试，验证整个流程
4. 准备进入阶段 4（MinerU 双引擎集成）


